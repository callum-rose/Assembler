namespace Assembler.Behaviours.Flow
{
	/// <summary>Iterates a int list when Executed, firing listeners once per element. See <see cref="ListLoopTrigger{T}"/>.</summary>
	public class IntListLoopTrigger : ListLoopTrigger<int> { }
}
