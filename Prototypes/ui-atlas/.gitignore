# Build artefacts of build-atlas.mjs / build-artboards.mjs. The sources are
# atlas.data.mjs (geometry) and the two build scripts; everything here is
# regenerated by running them.

# The seeded design canvas — 2.4 MB, almost all of it the editor payload.
shell-ui-atlas.html

# Intermediates: the headless-Chrome wrapper the PNG is rasterised through, and
# the sheet fragments the artboards embed.
raster.html
*.frag.svg
